
// Placeholder for seller controller logic
exports.getSellerInfo = (req, res) => {
  res.status(200).json({ success: true, message: "Seller info endpoint" });
};

exports.updateSellerInfo = (req, res) => {
    res.status(200).json({ success: true, message: "Seller info updated" });
};
